# 📖 الدليل التفصيلي الكامل لنشر متجر أريسا على Vercel

> **آخر تحديث:** دليل شامل خطوة بخطوة من الصفر حتى الموقع المباشر
> **المستوى:** مبتدئ — لا يحتاج خبرة تقنية

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المتطلبات قبل البدء](#المتطلبات-قبل-البدء)
3. [الخطوة ١: إنشاء حساب Vercel](#الخطوة-١-إنشاء-حساب-vercel)
4. [الخطوة ٢: إنشاء قاعدة بيانات PostgreSQL](#الخطوة-٢-إنشاء-قاعدة-بيانات-postgresql)
5. [الخطوة ٣: استيراد المشروع من GitHub](#الخطوة-٣-استيراد-المشروع-من-github)
6. [الخطوة ٤: إعداد متغيرات البيئة](#الخطوة-٤-إعداد-متغيرات-البيئة)
7. [الخطوة ٥: إعدادات البناء](#الخطوة-٥-إعدادات-البناء)
8. [الخطوة ٦: النشر](#الخطوة-٦-النشر)
9. [الخطوة ٧: التحقق من الموقع](#الخطوة-٧-التحقق-من-الموقع)
10. [الخطوة ٨: إضافة نطاق مخصص](#الخطوة-٨-إضافة-نطاق-مخصص)
11. [الخطوة ٩: الصيانة والتحديثات](#الخطوة-٩-الصيانة-والتحديثات)
12. [حل المشاكل الشائعة](#حل-المشاكل-الشائعة)
13. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## نظرة عامة

متجر **أريسا** متجر تجميل إلكتروني متكامل مبني بـ:
- **Next.js 16** (إطار العمل)
- **PostgreSQL** (قاعدة البيانات — عبر Vercel Postgres)
- **Prisma ORM** (إدارة قاعدة البيانات)
- **Tailwind CSS 4 + shadcn/ui** (التصميم)

المشروع مرفوع بالفعل على GitHub:
```
https://github.com/mohammed777749/arisa-beauty-store
```

**ما سيحدث تلقائياً عند النشر:**
- ✅ اكتشاف نوع قاعدة البيانات وتبديل المخطط
- ✅ إنشاء الجداول (`db push`)
- ✅ تهيئة البيانات الأولية (٦ فئات، ٣٢ منتجاً، ١٧٢ مراجعة، ١٨ خدمة)
- ✅ بناء وتشغيل الموقع

---

## المتطلبات قبل البدء

قبل البدء، تأكدي من:

| المتطلب | الحالة |
|---------|--------|
| حساب GitHub | ✅ المشروع مرفوع بالفعل |
| بريد إلكتروني صالح | لإنشاء حساب Vercel |
| متصفح إنترنت حديث | Chrome, Firefox, Safari, Edge |
| ١٥ دقيقة من الوقت | لاستكمال كل الخطوات |

**لا تحتاجين:**
- ❌ خبرة برمجية
- ❌ تثبيت أي برامج
- ❌ بطاقة ائتمانية (الخطة المجانية تكفي)

---

## الخطوة ١: إنشاء حساب Vercel

### ١.١ اذهبي إلى Vercel

افتحي المتصفح وادخلي على:
```
https://vercel.com/signup
```

### ١.٢ سجّلي بالـ GitHub

1. اضغطي زر **"Sign Up With GitHub"** (الأزرق الكبير)
2. سيُفتح صفحة GitHub — أدخلي بياناتكِ
3. اضغطي **"Authorize Vercel"** للسماح لـ Vercel بالوصول لمستودعاتك
4. ارجعي لـ Vercel — ستجدين نفسكِ مسجّلة

> 💡 **ملاحظة:** لو لديكِ حساب Vercel بالفعل، فقط سجّلي الدخول من:
> `https://vercel.com/login`

### ١.٣ أكملي إعداد الحساب

1. أكدي اسم المستخدم (`mohammed777749` — سيظهر تلقائياً)
2. اختاري **"Personal Account"** (حساب شخصي)
3. اضغطي **"Continue"**
4. اختاري **"Skip"** للأسئلة عن الفريق (لستِ بحاجة لها)

---

## الخطوة ٢: إنشاء قاعدة بيانات PostgreSQL

عندكِ خياران — اختاري الأنسب:

### 🅰️ الخيار أ: Vercel Postgres (الأسهل — موصى به)

#### ٢.أ.١ ادخلي للوحة التحكم

بعد تسجيل الدخول، اذهبي إلى:
```
https://vercel.com/dashboard
```

#### ٢.أ.٢ أنشئي قاعدة بيانات

1. من القائمة الجانبية (يمين)، اضغطي **"Storage"**
2. اضغطي زر **"Create Database"** (أزرق، أعلى يمين)
3. اختاري **"Postgres"** (الخيار الأول)
4. املئي البيانات:
   - **Database Name:** `arisa-db`
   - **Region:** `Singapore (sin1)` — أو الأقرب لكِ
   - **Plan:** `Hobby` (مجاني)
5. اضغطي **"Create"**

#### ٢.أ.٣ انتظري الإنشاء

سيظهر شريط تقدم — انتظري ٣٠ ثانية إلى دقيقة.

#### ٢.أ.٤ انسخي رابط الاتصال

بعد الإنشاء:
1. اضغطي على اسم القاعدة `arisa-db`
2. اذهبي لتبويب **".env"** أو **"Connection String"**
3. انسخي القيمة بجانب `DATABASE_URL` — ستبدو هكذا:
   ```
   postgresql://default:AbCdEf123456@ep-xxx-sin1.postgres.vercel-storage.com/verceldb
   ```
4. **احفظيها في مكان آمن** — ستحتاجينها في الخطوة ٤

---

### 🅱️ الخيار ب: Neon (مجاني للأبد — بديل)

إذا فضّلتي مزوداً خارجياً:

#### ٢.ب.١ سجّلي على Neon

1. اذهبي إلى `https://neon.tech`
2. اضغطي **"Sign Up"** → سجّلي بالـ GitHub
3. اضغطي **"Create New Project"**
4. سمّيه `arisa-db`، اختاري Region `Asia Southeast (Singapore)`
5. اضغطي **"Create Project"**

#### ٢.ب.٢ انسخي رابط الاتصال

1. بعد الإنشاء، ستظهر صفحة بـ connection strings
2. انسخي **"Pooled connection"** (يبدأ بـ `postgresql://`)
3. احفظيه — ستحتاجينه في الخطوة ٤

---

## الخطوة ٣: استيراد المشروع من GitHub

### ٣.١ ابدئي مشروعاً جديداً

1. اذهبي إلى `https://vercel.com/new`
2. ستجدين قسم **"Import Git Repository"**
3. ابحثي عن `arisa-beauty-store` في مربع البحث
4. اضغطي **"Import"** بجانب المستودع

> 💡 لو لم تجديه، تأكدي أن Vercel لديه صلاحية الوصول. اضغطي
> **"Adjust GitHub App Permissions"** ومنحي الصلاحية للمستودع.

### ٣.٢ صفحة إعداد المشروع

ستظهر صفحة **"Configure Project"** بها:

- **Framework Preset:** Next.js (سيُكشف تلقائياً ✓)
- **Root Directory:** `./` (اتركيه افتراضياً)
- **Build Command:** `bun run vercel-build` (أو اتركيه افتراضياً)
- **Output Directory:** `.next` (افتراضي)
- **Install Command:** `bun install` (أو `npm install`)

**لا تغيّري شيئاً هنا** — اتركيها كما هي. كل الإعدادات مدمجة في `vercel.json` و `package.json`.

---

## الخطوة ٤: إعداد متغيرات البيئة

### ٤.١ افتحي قسم Environment Variables

في نفس صفحة "Configure Project"، انزلي لأسفل حتى تجدي:
```
Environment Variables
```

### ٤.٢ أضيفي متغير DATABASE_URL

1. اضغطي في مربع **"Name"** واكتبي:
   ```
   DATABASE_URL
   ```

2. اضغطي في مربع **"Value"** والصقي رابط PostgreSQL الذي نسختِه من الخطوة ٢:
   ```
   postgresql://default:AbCdEf123456@ep-xxx-sin1.postgres.vercel-storage.com/verceldb
   ```

3. في قسم **"Environment"**، تأكدي أن كل الخيارات مفعّلة:
   - ✅ Production
   - ✅ Preview
   - ✅ Development

4. اضغطي زر **"Add"** (أزرق)

> ⚠️ **مهم:** `DATABASE_URL` هو المتغير الوحيد المطلوب. كل الإعدادات الأخرى (الشعار، الهاتف، الواتساب) مدمجة في الكود.

---

## الخطوة ٥: إعدادات البناء

### ٥.١ تحققي من Build Command

في قسم **"Build and Output Settings"** (قد تكون مطوية — اضغطي عليها لتوسيعها):

| الحقل | القيمة |
|-------|--------|
| Build Command | `bun run vercel-build` (اتركيه فارغاً = افتراضي) |
| Output Directory | `.next` (افتراضي) |
| Install Command | `bun install` (أو اتركيه فارغاً) |

> 💡 **لو ظهر خطأ "bun not found"** أثناء البناء، غيّري Install Command إلى `npm install` و Build Command إلى `npm run vercel-build`.

### ٥.٢ لا تغيّري أي شيء آخر

كل الإعدادات الأخرى مُدارة تلقائياً عبر:
- `vercel.json` (الإعدادات العامة)
- `package.json` (السكريبتات)
- `next.config.ts` (إعدادات Next.js)

---

## الخطوة ٦: النشر

### ٦.١ اضغطي Deploy

انزلي لأ أسفل الصفحة واضغطي الزر الأزرق الكبير:
```
🚀 Deploy
```

### ٦.٢ تابعي عملية البناء

ستظهر صفحة بها سجل البناء المباشر. ستشاهدين:

```
[auto-db] نوع قاعدة البيانات المكتشف: postgresql
[auto-db] تم استخدام مخطط PostgreSQL
[auto-db] تشغيل prisma generate...
✔ Generated Prisma Client

[auto-db] دفع المخطط إلى قاعدة البيانات (db push)...
[auto-db] ✅ تم دفع المخطط بنجاح

[auto-db] تهيئة البيانات الأولية (seed)...
🌱 Seeding database...
  ✓ 6 categories
  ✓ 32 products
  ✓ 172 reviews
  ✓ 18 services
✅ Seeding complete!

[auto-db] ✅ تمت التهيئة التلقائية للبيانات الأولية
[auto-db] ✅ اكتملت التهيئة التلقائية لقاعدة البيانات

Running "next build"...
 ✓ Compiled successfully
 ✓ Route (pages)                     │  Size
 ✓ /                                 │  42 kB
 ✓ /api/products                     │  1.2 kB
 ...
✓ Build complete!
```

### ٦.٣ انتظري اكتمال النشر

العملية تستغرق **٢-٥ دقائق**. ستجدين مؤشر تقدم. عند الانتهاء سترين:

```
🎉 Congratulations! Your project has been deployed.
```

مع زر **"Visit"** للذهاب للموقع.

> ⚠️ **لو فشل البناء:** لا تقلقي — انظري قسم [حل المشاكل](#حل-المشاكل-الشائعة) أدناه.

---

## الخطوة ٧: التحقق من الموقع

### ٧.١ افتحي الموقع

اضغطي **"Visit"** أو اذهبي للرابط:
```
https://arisa-beauty-store.vercel.app
```
(أو رابط مشابه — Vercel يعطي اسماً عشوائياً)

### ٧.٢ قائمة التحقق

تأكدي أن كل التالي يعمل:

| العنصر | كيف تتحققين |
|--------|-------------|
| 🏠 الصفحة الرئيسية | تظهر بصور البانر والمنتجات |
| 🛒 المنتجات | اضغطي "تسوقي الآن" — تظهر ٣٢ منتجاً |
| 💅 الخدمات | اضغطي "خدماتنا" — تظهر ١٨ خدمة |
| 🔍 البحث | ابحثي عن "سيروم" في شريط البحث |
| 📦 صفحة منتج | اضغطي أي منتج — تظهر التفاصيل والمراجعات |
| 🛒 السلة | أضيفي منتجاً للسلة — يظهر في الأعلى |
| 🌙 الوضع الليلي | اضغطي أيقونة القمر في الهيدر |
| 💚 زر الواتساب | يظهر في أسفل اليمين — يفتح واتساب |
| 📞 رقم الهاتف | يظهر في الفوتر |
| 🎛️ لوحة التحكم | اذهبي لـ `?view=admin` — كلمة السر `admin1234` |

### ٧.٣ تحققي من لوحة التحكم

1. أضيفي `?view=admin` للرابط:
   ```
   https://your-app.vercel.app/?view=admin
   ```
2. أدخلي كلمة السر: `admin1234`
3. تحققي من:
   - ✅ لوحة المعلومات تعرض الإحصائيات
   - ✅ تبويب المنتجات يعرض ٣٢ منتجاً
   - ✅ تبويب الخدمات يعرض ١٨ خدمة
   - ✅ تبويب الطلبات يعمل

### ٧.٤ غيّري كلمة سر الأدمن

**مهم جداً للأمان:**
1. في لوحة التحكم، اضغطي **"تغيير كلمة السر"** في الشريط الجانبي
2. أدخلي كلمة السر الحالية: `admin1234`
3. أدخلي كلمة سر جديدة قوية (٦ أحرف على الأقل)
4. اضغطي **"حفظ"**

---

## الخطوة ٨: إضافة نطاق مخصص

### ٨.١ اذهبي لإعدادات النطاق

1. في لوحة تحكم Vercel، اضغطي على مشروع `arisa-beauty-store`
2. اذهبي لتبويب **"Settings"**
3. من القائمة الجانبية، اضغطي **"Domains"**

### ٨.٢ أضيفي نطاقك

1. اضغطي **"Add"**
2. اكتبي نطاقك (مثلاً): `arisa-salon.com`
3. اضغطي **"Add"**

### ٨.٣ حدّثي DNS

سيظهر Vercel سجلَّين DNS لإضافتهما عند مزوّد نطاقك:

| النوع | الاسم | القيمة |
|-------|-------|--------|
| A | `@` | `76.76.21.21` |
| CNAME | `www` | `cname.vercel-dns.com` |

أضيفيهما في لوحة تحكم مزوّد النطاق (Namecheap, GoDaddy, إلخ).

### ٨.٤ انتظري التفعيل

بعد تحديث DNS (من ٥ دقائق إلى ٤٨ ساعة):
- Vercel سيظهر علامة ✓ خضراء بجانب النطاق
- موقعك سيعمل على `https://arisa-salon.com`
- شهادة SSL تُفعّل تلقائياً ومجاناً

---

## الخطوة ٩: الصيانة والتحديثات

### ٩.١ تحديث الموقع

أي تغيير في الكود، فقط ارفعيه لـ GitHub:

```bash
git add -A
git commit -m "تحديث جديد"
git push origin main
```

Vercel سينشر التحديث **تلقائياً** خلال ٢-٣ دقائق.

### ٩.٢ مراقبة السجلات

1. في لوحة تحكم Vercel → مشروعك → **"Deployments"**
2. اضغطي على آخر نشر لمشاهدة السجلات
3. أو اذهبي لـ **"Logs"** لمشاهدة سجلات التشغيل المباشرة

### ٩.٣ إدارة قاعدة البيانات

#### عرض البيانات:
1. اذهبي لـ **"Storage"** → `arisa-db`
2. اضغطي **"Data"** لعرض الجداول والسجلات

#### نسخ احتياطي:
1. في صفحة القاعدة → **"Backups"**
2. اضغطي **"Create Backup"**

### ٩.٤ حدود الخطة المجانية

| المورد | الحد الشهري |
|--------|-------------|
| النطاق الترددي | 100 GB |
| وقت التشغيل (Serverless) | 100 ساعة |
| قاعدة البيانات | 256 MB |
| عدد النشرات | غير محدود |

> 💡 تكفي لـ ~١٠٠٠ زائر يومياً. للتوسع، ارتقي لخطة Pro ($20/شهر).

---

## حل المشاكل الشائعة

### 🔴 مشكلة: البناء فشل بـ "prisma generate"

**السبب:** Prisma لم يُولّد عميله.

**الحل:**
1. في Vercel → Settings → Build Command
2. غيّريها إلى:
   ```
   npx prisma generate && next build
   ```

---

### 🔴 مشكلة: "Database connection error"

**السبب:** `DATABASE_URL` غير صحيح أو القاعدة غير مهيّأة.

**الحل:**
1. تحققي من `DATABASE_URL` في Settings → Environment Variables
2. تأكدي أنه يبدأ بـ `postgresql://`
3. أعيدي النشر (Redeploy)

---

### 🔴 مشكلة: الموقع يفتح لكن لا توجد منتجات

**السبب:** قاعدة البيانات فارغة.

**الحل التلقائي:** اضغطي أي صفحة — الموقع سيهيّئ البيانات تلقائياً.

**الحل اليدوي:**
```bash
curl https://your-app.vercel.app/api/seed
```

---

### 🔴 مشكلة: "bun: command not found"

**السبب:** Vercel لا يدعم bun افتراضياً.

**الحل:**
1. Settings → Install Command: `npm install`
2. Settings → Build Command: `npm run vercel-build`
3. أعيدي النشر

---

### 🔴 مشكلة: "Module not found" بعد تحديث

**السبب:** الـ dependencies غير متزامنة.

**الحل:**
1. في Vercel → Deployments
2. اضغطي على آخر نشر
3. اضغطي **"Redeploy"** مع تفعيل **"Use existing Build Cache"** ❌ (عطّليه)

---

### 🔴 مشكلة: بطء في الاستجابة

**السبب:** المنطقة بعيدة عن زوّارك.

**الحل:**
1. عدّلي `vercel.json` — غيّري `regions`:
   - `sin1` = سنغافورة (آسيا)
   - `iad1` = شرق أمريكا
   - `sfo1` = غرب أمريكا
   - `fra1` = فرانكفورت (أوروبا)
2. ارفعي التحديث لـ GitHub

---

### 🔴 مشكلة: الصور المرفوعة لا تظهر

**السبب:** حجم الصورة كبير جداً.

**الحل:**
- الحد الأقصى للصور المرفوعة: ١.٥ ميجابايت
- استخدمي صوراً أصغر، أو روابط خارجية بدلاً من الرفع

---

## الأسئلة الشائعة

### ❓ هل أحتاج بطاقة ائتمانية؟
**لا.** الخطة المجانية (Hobby) كافية ولا تتطلب بطاقة.

### ❓ هل سيتوقف الموقع يوماً؟
**لا.** Vercel يعمل ٢٤/٧ على الخطة المجانية. لكن لو تجاوزتِ الحدود، سيظهر إعلان "Rate Limited" مؤقتاً.

### ❓ هل بياناتي آمنة؟
**نعم.** قاعدة البيانات على Vercel Postgres مشفّرة، وكل البيانات في `localStorage` تبقى على جهاز المستخدم.

### ❓ كيف أغيّر الشعار أو رقم الهاتف؟
عدّلي ملف `src/lib/brand.ts` محلياً، ثم ارفعي لـ GitHub:
```bash
git add src/lib/brand.ts
git commit -m "تحديث الهوية"
git push origin main
```

### ❓ كيف أضيف منتجات جديدة؟
1. اذهبي للوحة التحكم: `?view=admin`
2. أدخلي كلمة السر
3. تبويب "المنتجات" → "إضافة منتج"
4. املئي البيانات واضغطي "إضافة المنتج"

### ❓ هل يمكنني تغيير كلمة سر الأدمن؟
**نعم.** في لوحة التحكم → "تغيير كلمة السر". لو نسيتِها، اضغطي "نسيت كلمة السر؟ إعادة التعيين" لتعود لـ `admin1234`.

### ❓ كم منتجاً/خدمة يدعم الموقع؟
**غير محدود** — حسب حجم قاعدة البيانات (٢٥٦ ميجابايت في الخطة المجانية = آلاف المنتجات).

### ❓ هل يدعم الدفع الإلكتروني؟
حالياً يدعم: الدفع عند الاستلام، تحويل بنكي، بطاقة ائتمانية (محاكاة). للدفع الحقيقي، أضيفي Moyasar أو Tap Payments.

### ❓ كيف أرى عدد الزوار؟
1. في Vercel → مشروعك → **"Analytics"**
2. فعّليها (مجانية في Hobby)
3. ستشاهدين إحصائيات يومية

---

## ✅ قائمة التحقق النهائية

قبل اعتبار الموقع "منشوراً" تأكدي:

- [ ] حساب Vercel مُنشأ ومربوط بـ GitHub
- [ ] قاعدة بيانات PostgreSQL مُنشأة
- [ ] `DATABASE_URL` مضاف في Environment Variables
- [ ] تم النشر بنجاح (لا أخطاء في السجلات)
- [ ] الموقع يفتح ويظهر المنتجات
- [ ] لوحة التحكم تعمل (`?view=admin`)
- [ ] كلمة سر الأدمن غُيّرت من الافتراضية
- [ ] زر الواتساب يعمل
- [ ] الوضع الليلي يعمل
- [ ] النطاق المخصص مُضاف (اختياري)

---

## 📞 الدعم

- **مشاكل Vercel:** [vercel.com/help](https://vercel.com/help)
- **مشاكل قاعدة البيانات:** راجعي سجلات Vercel → Logs
- **مشاكل الكود:** ارفعي issue على GitHub

---

## 🎉 تهانينا!

لو وصلتِ لهنا، متجرك **أريسا** يعمل مباشرة على الإنترنت! 🌹

شاركي الرابط مع صديقاتكِ وعميلاتكِ، وابدئي رحلة التجارة الإلكترونية.

**رابط متجركِ:** `https://arisa-beauty-store.vercel.app` (أو نطاقك المخصص)

---

*آخر تحديث لهذا الدليل: متجدد مع كل تحديث للمشروع*
